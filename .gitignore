node_modules/
.next/
out/
build/
*.tsbuildinfo
next-env.d.ts

# runtime state — collections + settings live here, never committed
# (anchored to root — src/data/ holds committed static artifacts, e.g. the
# Wikidata artist-movements lookup, and must NOT be swept up by this rule)
/data/

# env
.env*.local

.DS_Store
.gstack/
